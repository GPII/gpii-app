{
    "type": "gpii.tests.dev.config",
    "options": {
        "gradeNames": ["fluid.component"],
        "distributeOptions": {
            "distributeMenuDisplay": {
                "record": "gpii.app.menuInAppDev",
                "target": "{that menu}.options.gradeNames"
            }
        }
    },
    "mergeConfigs": [
        "./gpii.tests.all.config.json"
    ]
}
