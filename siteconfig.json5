{
    // Whether to disable the QSS save button.
    // `true` if the button should be disabled and `false` otherwise.
    disableQssSaveButton: false,

    // The scaling factor for the QSS
    qssScaleFactor: 0.8,

    // The scaling factor for the PSP
    pspScaleFactor: 0.75
}
